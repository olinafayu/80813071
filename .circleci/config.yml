version: 2.1
description: |
  An orb for working with Amazon Elastic Container Service (ECS).
  Supports the EC2 and Fargate launch types and Blue/Green deployment
  via CodeDeplo


jobs:
  build:
    docker:
      - image: docker:18.06.3-ce-git
    steps:
      - setup_remote_docker
      - checkout
      - run: docker run -e CI=true trendava/node:latest
      - run: CI=true 
  test:
    docker:
      - image: docker:18.06.3-ce-git
    steps:
      - setup_remote_docker
      - checkout
      - run: docker run -e CI=true trendava/node:latest
      - run: CI=true 
  client-test:
    docker:
      - image: docker:18.06.3-ce-git
    steps:
      - setup_remote_docker
      - checkout
      - run: docker run -e CI=true trendava/node:latest
      - run: CI=true 
  server-test:
    docker:
      - image: docker:18.06.3-ce-git
    steps:
      - setup_remote_docker
      - checkout
      - run: docker run -e CI=true trendava/node:latest
      - run: CI=true 
workflows:
  version: 2
  build-test-and-lint:
    jobs:
      - build
      - server-test
      - client-test
      - test:
          requires:
            - server-test
